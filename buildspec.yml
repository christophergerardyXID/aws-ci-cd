version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

  build:
    commands:
      - echo "Building the project wazaaaa"
      - npm install
      - npm run build
      - ls

artifacts:
  files:
    - "dist/**/*"
  base-directory: "dist"
  discard-paths: no
